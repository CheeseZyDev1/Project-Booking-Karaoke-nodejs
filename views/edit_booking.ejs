<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Booking</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        body {
            padding-top: 60px; /* Adjust based on navbar height */
            padding: 20px;
            margin: 0;
        }
        h1 {
            text-align: center;
            margin-bottom: 20px;
        }
        .form-border {
            border: 1px solid #ccc;
            border-radius: 10px;
            padding: 20px;
            background-color: #f9f9f9;
            max-width: 500px;
            margin: 0 auto;
        }
        .form-border label {
            display: block;
            font-weight: bold;
            margin-bottom: 10px;
        }
        .form-border input[type="datetime-local"],
        .form-border select {
            width: 100%;
            padding: 8px;
            border-radius: 5px;
            border: 1px solid #ccc;
            box-sizing: border-box;
            margin-bottom: 10px;
        }
        .form-border button[type="submit"] {
            padding: 10px 20px;
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        .form-border button[type="submit"]:hover {
            background-color: #0056b3;
        }
        input[type="date"],
        select {
            width: 100%;
            padding: 10px;
            border-radius: 5px;
            border: 1px solid #ccc;
            box-sizing: border-box;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary fixed-top">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">Navbar</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="#">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Features</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Pricing</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link disabled" aria-disabled="true">Disabled</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container"><br><br>
        <h1><%= booking ? "Edit" : "Add" %> Booking</h1>
        <div class="form-border">
            <form action="<%= booking ? `/booking/update/${booking.BookingID}` : '/bookings/create' %>" method="post">
                <!-- Fields for selecting User, Room, StartTime, EndTime, and Status -->
                <!-- This is a simplified version. In a real application, you'd likely use <select> elements populated with users and rooms. -->
                <div class="mb-3">
                    <label for="startTime">Start Time:</label>
                    <input type="date" id="startTime" name="starttime" value="<%= booking ? booking.StartTime : '' %>" required>
                </div>
                <div class="mb-3">
                    <label for="endTime">End Time:</label>
                    <input type="date" id="endTime" name="endtime" value="<%= booking ? booking.EndTime : '' %>" required>
                </div>
                <label for="status">Status:</label>
                <select id="status" name="Status" required>
                    <option value="Pending" <%= booking && booking.Status === 'Pending' ? 'selected' : '' %>>Pending</option>
                    <option value="Confirmed" <%= booking && booking.Status === 'Confirmed' ? 'selected' : '' %>>Confirmed</option>
                    <option value="Cancelled" <%= booking && booking.Status === 'Cancelled' ? 'selected' : '' %>>Cancelled</option>
                </select>
                <div class="mb-3">
                    <label for="userId">User ID:</label>
                    <input type="text" id="userId" name="userId" value="<%= booking.UserID %>" class="form-control" required>
                </div>
                <div class="mb-3">
                    <label for="roomId">Room ID:</label>
                    <input type="text" id="roomId" name="roomId" value="<%= booking.RoomID %>" class="form-control" required>
                </div>
            
                <button type="submit"><%= booking ? "Update" : "Create" %></button>
            </form>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>